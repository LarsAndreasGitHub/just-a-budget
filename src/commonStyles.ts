import { StyleSheet } from 'react-native';

const commonStyles = StyleSheet.create({
    textInput: {
        height: 60,
        width: 300,
        borderColor: 'black',
        borderWidth: 2,
        fontFamily: 'Roboto',
        textAlign: 'center',
        fontSize: 40,
    },
});

export default commonStyles;